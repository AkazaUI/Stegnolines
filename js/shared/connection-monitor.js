/**
 * STEGNOLINES — Automated Connection & Latency Monitor
 * Monitors connectivity and redirects to offline.html if network is down or extremely slow.
 */
(function() {
  // Internet connection checking is disabled
  return;
})();
